{% extends 'base.html.twig' %}

{% block title %}Modifier sortie{% endblock %}

{% block body %}
    <h1>Modifier la sortie {{ sortie.titre }}</h1>

    {{ include('sortie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_sortie_index') }}" class="btn btn-secondary mt-3">Retour à la liste des sortie</a>

    {{ include('sortie/_delete_form.html.twig') }}
{% endblock %}
