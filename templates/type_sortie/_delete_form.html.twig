<form method="post" action="{{ path('app_type_sortie_delete', {'id': type_sortie.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_sortie.id) }}">
    <button class="btn">Supprimer la catégorie</button>
</form>
