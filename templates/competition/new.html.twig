{% extends 'base.html.twig' %}

{% block title %}Nouvelle Compétition - Écuries des 4 Routes{% endblock %}

{% block body %}
    <section class="py-5">
        <div class="container">
            {# En-tête avec style moderne #}
            <div class="text-center mb-5">
                <h1 class="text-primary-brand mb-3">
                    <i class="bi bi-plus-circle" style="color: var(--accent-color);"></i>
                    Ajouter une Compétition
                    <i class="bi bi-plus-circle" style="color: var(--accent-color);"></i>
                </h1>
                <div style="height: 3px; width: 200px; background: linear-gradient(90deg, var(--primary-color), var(--accent-color), var(--primary-color)); margin: 0 auto; border-radius: 2px;"></div>
                <p class="mt-3" style="color: var(--text-secondary);">
                    Créez une nouvelle compétition équestre
                </p>
            </div>

            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="card-modern">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h3 class="text-primary-brand mb-0">
                                <i class="bi bi-form me-2"></i>Informations de la compétition
                            </h3>
                            <span class="badge bg-success">
                                <i class="bi bi-plus-circle me-1"></i>Nouveau
                            </span>
                        </div>

                        <div class="form-text-modern mb-4">
                            <i class="bi bi-info-circle me-2"></i>
                            Remplissez les informations ci-dessous pour créer une nouvelle compétition.
                            Les champs marqués d'un astérisque (*) sont obligatoires.
                        </div>

                        {{ include('competition/_form.html.twig') }}
                    </div>

                    <div class="card-modern mt-4">
                        <div class="d-flex flex-wrap gap-3 justify-content-center">
                            <a href="{{ path('app_competition_index') }}" class="btn btn-outline-modern">
                                <i class="bi bi-arrow-left me-2"></i>Retour à la liste
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}