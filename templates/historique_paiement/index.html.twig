{% extends 'base.html.twig' %}

{% block title %}Historique des paiements{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">🧾 Mon historique de paiements</h1>

    {% if logs is empty %}
        <div class="alert alert-info">Aucun paiement enregistré pour le moment.</div>
    {% else %}
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Sortie</th>
                    <th>Détail</th>
                </tr>
            </thead>
            <tbody>
                {% for log in logs %}
                    <tr>
                        <td>{{ log.date|date('d/m/Y H:i') }}</td>
                        <td>
                            {% if log.type == 'paiement_annule' %}
                                ❌ Annulé
                            {% elseif log.type == 'paiement_reussi' %}
                                ✅ Réussi
                            {% else %}
                                {{ log.type }}
                            {% endif %}
                        </td>
                        <td>{{ log.sortie ? log.sortie.titre : '—' }}</td>
                        <td>{{ log.message }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}
</div>
{% endblock %}
