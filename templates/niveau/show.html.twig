{% extends 'base.html.twig' %}

{% block title %}Niveau{% endblock %}

{% block body %}
    <h1>Niveau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ niveau.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_niveau_index') }}">retour à la liste</a>

    <a href="{{ path('app_niveau_edit', {'id': niveau.id}) }}">modifier</a>


{% endblock %}
