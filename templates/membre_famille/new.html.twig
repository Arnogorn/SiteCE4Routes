{% extends 'base.html.twig' %}

{% block title %}Ajouter un membre de famille{% endblock %}

{% block body %}
    <div class="container mt-5">
        {# En-tête #}
        <div class="text-center mb-5">
            <h1 class="text-primary-brand mb-3">
                <i class="bi bi-person-plus" style="color: var(--accent-color);"></i>
                Ajouter un membre de famille
                <i class="bi bi-person-plus" style="color: var(--accent-color);"></i>
            </h1>
            <div style="height: 3px; width: 200px; background: linear-gradient(90deg, var(--primary-color), var(--accent-color), var(--primary-color)); margin: 0 auto; border-radius: 2px;"></div>
            <p class="mt-3" style="color: var(--text-secondary);">
                Ajoutez un membre de votre famille pour qu'il puisse participer aux sorties
            </p>
        </div>

        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card-modern">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3 class="text-primary-brand mb-0">
                            <i class="bi bi-form me-2"></i>Informations du membre
                        </h3>
                        <span class="badge bg-success">
                        <i class="bi bi-person-heart me-1"></i>Nouveau membre
                    </span>
                    </div>

                    <div class="form-text-modern mb-4">
                        <i class="bi bi-info-circle me-2"></i>
                        Remplissez les informations de votre membre de famille.
                        Les champs marqués d'un astérisque (*) sont obligatoires.
                    </div>

                    {{ include('membre_famille/_form.html.twig') }}
                </div>

                {# Retour #}
                <div class="card-modern mt-4">
                    <div class="text-center">
                        <a href="{{ path('app_membre_famille_index') }}" class="btn btn-outline-modern">
                            <i class="bi bi-arrow-left me-2"></i>Retour à la liste
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}